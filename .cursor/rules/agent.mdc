---
alwaysApply: true
---

# backend
- source `.venv` before running any command or script. Use `source .venv/bin/activate`.
- use `TMPDIR=./pip-cache` while installing `pip` packages. For e.g. `TMPDIR=./pip-cache pip install -r requirements.txt`
- update `backend/requirements.txt` when installing any new `pip` package

# frontend
- use typescript
- try to reuse components
- do not launch the vite server. I am already running it in the background.
- test the frontend changes at `http://localhost:5173/` using the `browsermcp`. Assume the server is already running.

# alias
- `vpp-web` refers to `/home/skumar/ext_ssd/vpp-web`
- `vpp-web/frontend` refers to `/home/skumar/ext_ssd/vpp-web/frontend/`


# ML code
- set `HF_HOME` to `backend/.hf-cache` before running `backend/inpaint.py`
- prefer `safetensors` over `bin` weight files